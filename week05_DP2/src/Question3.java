
public class Question3 {
    //The Abstract factory design pattern is based around the idea of a main factory that creates other factory.
    //A factory of factories. When adopting an abstract factory, a particular framework is initiated,
    //which creates objects adhering to a particular style and at runtime this factory is paired with any
    //concrete factory to produce
    //objects that follow the main overarching pattern.
}
